<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.etl</groupId>
        <artifactId>flink-etl-tool</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>flink-etl-client</artifactId>
    <name>Flink ETL Client</name>
    <description>客户端启动器，打包所有插件</description>

    <dependencies>
        <!-- 核心框架 -->
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Source 插件 -->
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-source-mysql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-source-localfile</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Sink 插件 -->
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-sink-console</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-sink-mysql</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Transform 插件 -->
        <dependency>
            <groupId>com.etl</groupId>
            <artifactId>flink-etl-transform</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Flink Table Planner Loader - 运行时需要，提供 ExecutorFactory 实现 -->
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-table-planner-loader</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Maven Shade Plugin: 打包所有依赖 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.etl.client.EtlClient</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>module-info.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>